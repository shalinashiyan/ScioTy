<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#FFFFFF">

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:layout_marginTop="50dp"
        android:src="@drawable/scioty_logo" />

    <ProgressBar
        android:id="@+id/progress_skip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="20dp"
        android:visibility="gone" />

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical">

        <Button
            android:id="@+id/btnskipfornow"
            style="@style/textFont"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/settings_title_bg"
            android:paddingTop="10dip"
            android:text="@string/specBtnSkipForNowText"
            android:textColor="@android:color/white"
            android:textSize="@dimen/title" />

        <Space
            android:layout_width="4dp"
            android:layout_height="6dp"
            android:layout_weight="2"></Space>

        <Button
            android:id="@+id/btnupgrade"
            style="@style/textFont"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bttm_bttn_bg"
            android:paddingTop="10dip"
            android:text="@string/specBtnUpgradeToProText"
            android:textColor="@android:color/white"
            android:textSize="@dimen/title" />


    </LinearLayout>

</RelativeLayout>